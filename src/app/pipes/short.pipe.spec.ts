/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ShortPipe } from './short.pipe';

describe('Pipe: Shorte', () => {
  it('create an instance', () => {
    let pipe = new ShortPipe();
    expect(pipe).toBeTruthy();
  });
});
