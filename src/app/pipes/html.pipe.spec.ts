/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HtmlPipe } from './html.pipe';

describe('Pipe: Htmle', () => {
  it('create an instance', () => {
    let pipe = new HtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
