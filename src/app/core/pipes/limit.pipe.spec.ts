/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LimitPipe } from './limit.pipe';

describe('Pipe: Limite', () => {
  it('create an instance', () => {
    let pipe = new LimitPipe();
    expect(pipe).toBeTruthy();
  });
});
